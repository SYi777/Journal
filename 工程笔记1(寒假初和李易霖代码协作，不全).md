1\.15

试图加上nx通信，添加nx

Communicate文件但是nx发东西stm32收不到进不了中断，usb转ttl也收不到

多出来的bug还有小陀螺，会锁定开小陀螺前一秒的状态，比如前一秒在转拨弹轮，这个时候开小陀螺，拨弹轮不会停，如果开了小陀螺，整个车除了移动就不听使唤了

Shoot task 422行，进自动模式后全程开摩擦轮

去掉了一个可以放进shoottask里面的子任务

给gimbaltask加上了自瞄接口，但是nx发的东西我现在收不到，这两天尝试解决

给底盘移植了键鼠wasd，ctrl小陀螺，c common，g 跟随，但是跟随的代码我还没改

串口可以正常收和解析，应该是nx的问题，nx问题解决了

里程碑有问题，打不了单发，只会抽动

1\.15 LYL

问题：进小陀螺后云台不动

解决：

	之前的小陀螺和robotAuto设置的全是rc\_p\->rc\.s\[0\]==2（对应右摇杆最下）

	我改成：robotAuto为rc\_p\->rc\.s\[1\]==2            【云台robot\_total\_mode\.c第85行】

问题：小陀螺时左右走反

解决：还没解决

1\.16 JYH

视觉串口下发接受执行调试正常

1\.17JYH

添加自瞄手动转换的逻辑，防止跳变

放弃里程碑，2006太快了用不了，修复单发bug，云台已经是完全体

