2026\.3\.22

1\.开始看数据结构与算法，一章

---
2026\.4\.2

1\.下载使用docker，toolkit，修改dockerfile，拉取构建镜像成功，检查ros节点以及发布消息，没有问题，但是发布命令飞机没法动，由于是在容器中使用，只能后台运行，没有图像，暂时不知道问题，疑似发布格式有毛病

---
2026\.4\.7 6：53

清明gap结束，写一下todolist，有太多事情要做了，先是课内的几门要准备期中考试，时间是4月26，很急啊，英语四级也要开始准备了，然后是抽一天去智源看看，还有是开始调轮腿，至少把电机驱动层先准备好，还有rmua的事情，现在刚弄完环境，什么都还没搞，然后广和通赛道的开发板还没申请，紫光同创的fpga比赛也才刚刚下好ide，基本理论一点没学，另外数据结构与算法也没开动太多，急急急

---
2026\.4\.8 22：37

1\.这几天在准备招新的事情

2\.今天看了学了一下lqr，找了一下8009电机的资料，，给工程开了（进度堪忧啊）

3\.学了一下Verilog，写了个流水灯，学习了怎么调试fpga，把集创赛的案例跑了一下，弄了一下hdmi输入输出的案例，和工程师交流了一下赛题细节

---
2026\.4\.12 20：41

1\.强化学习i2c读取写入hdmi芯片ms7200/7210的工程代码，理解ip核和代码模块的关系

2\.初步对解决赛题有对应思路，计划用fpga对图像进行灰度加上二值化，如果有能力最好做一些裁剪

3\.林宇恒负责做stm32h7的识别部分，但是只是尝试，成功概率不高

---
2026\.4\.13

1\.忙招新，补作业

2\.去了一趟海淀，和吴泓宇，刘崇讨论了一下去智源的必要性，我发现这个时候着急去的价值不大，考虑到还有期中和招新，以及个人能力的欠缺，还是先沉淀一下，补全一下算法和ros相关的东西

3\.从海淀实验室那边继承了一个电赛小车和一大堆元件

---
2026\.4\.14 10：05

1\.给香橙派烧录了系统

注：第二天换了gnome的桌面

2\.准备招新

---
2026\.4\.15 21：55

1\.面试一位信通的同学

2\.把车上的线接好了

3\.fpga成功跑完gamma和滤波的例程，进一步和林宇恒讨论了赛题，打算在fpga把边缘检测也做了

4\.移植了gamma的模块，成功跑通灰度化\+gamma\+滤波的完整流程，对fpga的文件构造更加了解

---
2026\.4\.16 22：00

1\.有灵感了给广和通想出一个合适的创新思路，具体见文档

2\.重新刷了香橙派换了gnome桌面，之前太难用了

3\.刷了点计网速通视频，预习一下

---
2026\.4\.17 23：23

1\.紫光同创的测试上周招新忘做了，明天补做，今天跑了网口的例程，做了一下锁相环配置移植，跑通了

---
2026\.4\.18 00：29

我需要思考一下我当下的安排，我的事情是非常巨量的，所以需要梳理一下

第一个是课内的任务需要完成，还要准备期中考试

第二个是紫光同创的比赛，fpga的测试和具体的实现，测试倒还好说，识别感兴趣区域和裁剪实在是很为难，在于我的专业方向不是fpga，并且我未来用到fpga的概率很低，另一个是没有v的基础很难读懂代码，只能凭借强化学习慢慢收敛，但实在是折磨

第三个是广和通，这个截止日期倒是比较晚，七月份，这个先糊弄吧

第四个是招新，这个在期中之前，也是个大问题

还有计导的大作业，贪吃蛇

---
2026\.4\.18 19：33

1\.把紫光同创的初试测试过了，其实一开始就能过的，结果一直烧成另一个代码卡了很久

2\.把sobel和matrix膨胀的原理学了，并且在fpga上实现了功能

3\.通过计算边缘最长和长宽比例限制把roi做了，碍于时间短暂，只有简单的裁剪，中间由于写了嵌套1080和1920产生了大量加法器和比较器，布线直接爆了，优化为只记录最大值，一行一行算，正常通过，但是roi还需要调试

4\.采集卡快到了，争取尽快联调

---
2026\.4\.19 22：50

1\.今天把fpga的几个算法模块合并了，gamma矫正\+灰度化\+sobel\+二值化\+膨胀\+投影\+画框，几个功能齐全了，输出图像效果不错，但是车牌边缘比例检测有问题，框一直画不上

2\.对于roi目前除了加特征还有几个方法，一个是彻底不管交给arm跑识别

另一个是跑很简单cnn，但是空间太小而且成功率低，加上自己不熟悉，还有就是再优化一下算法，把腐蚀做了，还有投影也做一下优化，这个后面再说，还要准备期中

---
2026\.4\.20

1\.昨天没进度，复习去了	

---
2026\.4\.21 23：54

1\.忙活一晚上，尝试只保留竖线和横线，分别找边界，结果代码弄出来一直internal error过不了编译，很烦

2\.重新开工程，优化了一下腐蚀和膨胀，roi还是没用很绝望啊

---
2026\.4\.22

1\.复习

---
2026\.4\.23 21：12

1\.算法迭代一点：投影找边界竖直方向噪声太多，所以只做水平方向的裁切，然后侧向投影舍去最上面30横排和下面30横排的像素避开边框，中间找峰值时，读到一个高于周围较多的峰值时候，对两侧若干排内所有像素遍历找相似峰值，两个峰值差异在可接受范围内就认为是y1，y2（顺序贪婪算法）

跟上了！！！！！跟上车牌了！！！！虽然效果一般，一直在闪

---
2026\.4\.24 14：19

1\.算法优化：从上往下遍历找到一个峰值之后，在这个峰值向下较大范围里面找另一个相似峰值，比如说从该点搜索到最底下那一行，比较和当前峰的大小，如果和当前峰差异百分之20或更小，则认为是另一个边界，而且两个边界中间不会出现更高的峰（车牌中间投影值大但是均匀）

2\.给投影的值加上了滤波     半小时后：滤波没什么用，反而不利于寻找特征峰值

3\.试图加入类似双指针的思路

4\.我发现车牌下缘上面那一部分比较空，所以往竖直方向投影差异会很大

5\.车牌颜色向量也很明显：对于蓝牌：R<80,G<80,B>240;对于黄牌：R>240,G>135,B<20;对于绿牌：R>135,G>240,B>140;（采用相对关系防止光照影响）

6\.算法更新：先选颜色，找到颜色密集的中心点，往两侧选取边缘峰值

输入图像后先gamma补偿，然后高斯滤波，然后腐蚀/膨胀，然后sobel，然后腐蚀膨胀，最后到这投影，所以这个找颜色的module可以把线接到gauss滤波的那根上，挨个筛查rgb的分布和聚集位置，以蓝，绿，黄顺序挨个跑一次，如果颜色中线分布在1920\*1080周围过于靠边缘的位置，就放弃当前颜色，换下一个颜色找，最后输出值是一个x一个y，代表车牌中心，如果三个颜色都失效，就输出0，0，然后找边缘交给另一个模块，输出x1，y1，x2，y2，这个模块接入二值化的图像，以中心点开始往x和y两边找一样（差异百分之15以内）的峰值，找到了就框，找不到就画一个尽可能大的框（迭代思路）

Ai写的：

__功能说明__

1. __颜色中心有效__：
	- __第一帧__：累积全图行投影，以中心 cy 为起点，__向上__找第一个高亮峰，__向下__找第二个高亮峰，并校验两峰相似度 ≥ SIM\_THRESH%。
		- 成功 → 使用找到的 y1, y2 作为第二帧的行范围。
		- 失败 → 以 cy 为中心，半屏高 HALF\_SCREEN\_H 兜底。
	- __第二帧__：仅在 \[y1, y2\] 内累积列投影，以 cx 为中心向左/右找峰并校验相似度。
		- 成功 → 输出精确边界。
		- 失败 → 以 cx 为中心，半屏宽 HALF\_SCREEN\_W 兜底。
2. __颜色中心无效（0,0）__：直接输出全屏框 \[0, H\_ACT\-1\] × \[0, V\_ACT\-1\]。
3. __参数化设计__：半屏尺寸和相似度阈值可调，方便后期根据实际场景优化。

该模块可直接与 plate\_color\_center 级联，形成完整车牌定位流水线。

1. __颜色判据__：采用相对阈值（b > r \+ TH\_BLUE 等），避免绝对阈值受光照影响。
2. __并行投影__：在帧有效期间同时累加蓝、绿、黄三色的行列投影，帧结束时所有数据就绪。
3. __顺序分析__：按照蓝→绿→黄的顺序依次检查：
	- 扫描行投影找到最大值及其位置。
	- 扫描列投影找到最大值及其位置。
	- 检验行中心是否在有效区域（四周剔除 BORDER 像素）。
	- 检验行峰值周围 ±NEIGHBOR 范围内是否有足够数量的相似峰（大于等于 MIN\_NEIGHBOR）。
	- 同理检验列峰值。
	- 若某颜色满足所有条件，则输出其中心坐标并置 valid=1，不再检查后续颜色。
	- 若三种颜色都不满足，输出 \(0,0\) 且 valid=0

---
2026\.4\.25 10：35

1\.重新捋一下颜色识别的算法逻辑：首先是相对颜色关系选择还是绝对颜色选择，这两个其实就是判断逻辑问题，考虑到我们已经做过gamma补偿，所以先选绝对颜色

第二个是选择质心还是峰值，（算法优化）峰值不好检查，而且不利于综合，所以尝试移动窗口求投影之和算平均来做，一个固定20像素宽度窗口，计算该方向上向量符合的个数，这样就能找到/或者生成对应向量的掩膜然后投影

第三是全图扫描，如果在周围128就送原图的标志位，下一个module收到valid说明不在128，就扫描周围边界，扫不到给半图，扫到了给边界，如果没valid不用扫给原图

2\.第二个问题选择了掩膜，效果还不错，找到了车牌位置，画框也有了，位置差不多，但是受窗口边框干扰和内存问题，还需要优化

3\.（算法更新）识别到颜色中心后根据颜色面积给出参考框大小，同时valid给0，如果边界识别失败就用参考框加上一些宽度给容错

---
2026\.4\.26 19：20

1\. 按照阈值生成掩膜，并且记录第一次产生峰值的横纵坐标位置（滑动窗口滤波），边界为前五个像素均小于新的一个或后五个像素列小于前一个，记录并输出参考边界值，对边界值取平均数得到颜色中心

23：47

2\.反复优化，扩大了一点窗口，改了一下进入和退出的阈值，现在百分之九十的情况可以把点点在车牌上

3\.为了把框尽量画小，我把颜色识别读出来的参考边界和边界检查出来的对比，取平均数或者不等占比的加权得到最后画框大小

4\.注意时序逻辑，否则状态机会卡死

---
2026\.4\.27 1：08

1\.（算法更新）循环裁剪，第一次画框结束后，在框内再次跑一次颜色中心寻找和边界寻找，在输出一次框，这样重复固定次数可以排除干扰

2\.（算法更新）优化一下算法思路，从中间在y方向向上下识别，上下均会有一个谷值，这个两个差值不应超过百分之三十，然后各有一个峰值，超过谷值百分之六十，则认为是y1，y2，然后在y1y2之间做x的投影找所有，一旦y方向寻找失败就从中心向上向下分别扩散270，左右分别480

成功了，跟随很死，框画的相对大点，还有几个问题，第一个是加权更改会影响时序，另一个是对绿色车牌识别不精确

通过纯投影寻找边界的算法还需要一点优化

11：44

1. 研读规则后考虑更新一下颜色寻找的算法，考虑把rgb换成hsv

20：58

1. 通过大量取样优化颜色阈值
2. 在中心点对周围9个像素宽度取样，没有对应车牌颜色valid给0
3. 找到问题了，掩膜太大，因为给的是所有颜色掩膜的叠加而不是单独一个颜色判断
4. 今天的修改就到这吧，鲁棒性不足，白色干扰很大

---
2026\.4\.28 11：54

1\.开始在cv里调参

23：00

1\.把视频拍完了，开始写文档准备提交

劳动节gap一下，把贪吃蛇大作业写了

学习了一下广度优先搜索和深度优先搜索，做了大作业的第一版，效果一般，分数不稳定

---
2026\.5\.6  23：00

1\.优化了一下贪吃蛇

2\.军训很傻逼，浪费时间

3\.把fpga整个项目带上文档传到git上了

